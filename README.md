## Getting started

Requires Node 18+ and **pnpm** (other managers work, but the lockfile is pnpm).

```bash
pnpm install
pnpm dev          # http://localhost:3000
pnpm typecheck    # tsc --noEmit
pnpm lint
pnpm build && pnpm start
```